{
  "name": "@grove/deploy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Builds and pushes Docker images to GHCR and Docker Hub, creates GitHub releases",
  "scripts": {
    "deploy": "tsx src/index.ts"
  },
  "devDependencies": {
    "@grove/eslint-config": "workspace:*",
    "@grove/typescript-config": "workspace:*",
    "tsx": "~4.21.0"
  }
}

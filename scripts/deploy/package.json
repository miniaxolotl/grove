{
  "name": "@qdrant-memory/deploy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Builds and pushes Docker images to GHCR and Docker Hub, creates GitHub releases",
  "scripts": {
    "deploy": "tsx src/index.ts"
  },
  "devDependencies": {
    "@qdrant-memory/eslint-config": "workspace:*",
    "@qdrant-memory/typescript-config": "workspace:*",
    "tsx": "~4.21.0"
  }
}

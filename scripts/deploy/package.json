{
  "name": "@script/deploy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local Docker build and push script for development",
  "scripts": {
    "deploy": "tsx src/index.ts"
  },
  "devDependencies": {
    "@lib/eslint-config": "workspace:*",
    "@lib/typescript-config": "workspace:*",
    "tsx": "~4.21.0"
  }
}

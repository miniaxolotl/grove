{
  "name": "@grove/release",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Wraps Changesets version and publish workflow for npm releases",
  "scripts": {
    "release": "tsx src/index.ts"
  },
  "devDependencies": {
    "@grove/eslint-config": "workspace:*",
    "@grove/typescript-config": "workspace:*",
    "tsx": "~4.21.0"
  }
}
